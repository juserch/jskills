# {topic} PoC 验证模板

> 目标方案：{target_scheme}
> 生成时间：{date}
> 基于调研：[{report_title}]({report_link})
> Skeleton hypotheses 映射：{hypotheses_summary}

<!--
  FIR legend: [F]=Fact  [I]=Inference  [R]=Recommendation
  PoC 模板充满 [R]（验证动作）；允许 [R]，因为 PoC 是执行文档
  硬约束：每条假设必须有量化成功标准；时间边界 1-2 周；环境三块齐全
-->

<!--
  v3.4 增量（C18/C19）：
  - confidence 数字必须紧跟 {cal: <ref-class>} 或 {uncal} 标注；TL;DR 与 Outlook 段禁 {uncal}
  - 跨节单源 [F] 关节（C18 触发）且不可替代时，对应 section 末插 > [SINGLE_SOURCE_RISK]: ... 注解
-->

## 1. 验证目标（对齐 skeleton.hypotheses）

| 假设 id | 假设 | 成功标准（量化） | 验证方法 | 预期风险 |
|---------|------|----------------|---------|---------|
| H1 | {skeleton.hypotheses[0].statement} | {量化标准：例 "QPS > 5万 且 P99 < 80ms"} | {测试方法：压测 / 对照 / 消融} | {假设可能失败的方向} |
| H2 | {...} | {...} | {...} | {...} |
| H3 | {...} | {...} | {...} | {...} |

**不接受的成功标准**（会被拒绝）：

- ❌ "性能可接受" — 未量化
- ❌ "体验流畅" — 主观
- ❌ "稳定运行" — 未定义

## 2. 测试环境

```yaml
硬件:
  cpu: {规格 + 核数 + 主频}
  memory: {容量 + 代际}
  disk: {类型 + 容量 + IOPS}
  network: {带宽 + 拓扑}

软件:
  os: {版本 + kernel}
  runtime: {版本}
  dependencies:
    - {依赖 1: 版本}
    - {依赖 2: 版本}

数据:
  规模: {数据量}
  类型: {数据特征}
  来源: {生产副本 / 合成 / 公开数据集}
  许可: {数据合规说明}
```

## 3. 测试脚本

```bash
# 3.1 安装依赖
{install commands 可执行}

# 3.2 启动服务
{start commands}

# 3.3 准备测试数据
{data prep commands}

# 3.4 执行测试
{test commands + parameters}

# 3.5 收集结果
{metric collection commands}
```

**禁止伪代码**。每条命令必须可在目标环境直接执行。

## 4. 结果记录

| 指标 | 目标值 | 实际值 | 达成？ | 偏差原因 |
|------|-------|-------|--------|---------|
| {指标 1 — 例 P99 延迟} | < 80 ms | ___ | ☐ | ___ |
| {指标 2 — 例 QPS} | > 50,000 | ___ | ☐ | ___ |
| {指标 3 — 例 成本 $/百万请求} | < $0.50 | ___ | ☐ | ___ |

## 5. 结论（对齐 hypotheses 验证状态）

- [ ] **所有假设验证通过**：推进到小范围试点
- [ ] **部分假设失败**：调整方案 / 降配参数 / 换选型
- [ ] **验证失败**：重新评估方案适用性；可能退回到方案 B/C
- [ ] **待定**：环境或数据有限导致无法结论；需补充 {缺什么}

## 6. 时间边界

| 阶段 | 内容 | 时间 | 负责人 |
|------|------|------|-------|
| 1 | 环境搭建 | 1-2 天 | {TBD} |
| 2 | 执行测试 | 3-5 天 | {TBD} |
| 3 | 结果分析 | 1-2 天 | {TBD} |
| 4 | 结论 + 下一步 | 0.5 天 | {TBD} |

**总时间**：1-2 周。超时直接标"验证超时"，不拖延。

## 7. 若验证通过，下一步

[R] {具体 next action — 例 "在 5% 流量灰度 2 周"}
[R] {监控指标 — 例 "P99 / 错误率 / 成本对比"}
[R] {回滚条件 — 例 "P99 > 100ms 或 错误率 > 0.5% 立即回滚"}

## 8. 若验证失败，下一步

[R] {重新评估的具体议程 — 例 "换选型到方案 B，重启 PoC"}
[R] {学到的教训 — 归档到调研报告的"待验证假设"段}

---

> Generated by [forge/insight-fuse](https://github.com/juserai/forge) — PoC template output
