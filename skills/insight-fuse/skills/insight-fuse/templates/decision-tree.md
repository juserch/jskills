# {topic} 选型决策树

> 生成时间：{date}
> 基于调研：[{report_title}]({report_link})
> 研究类型：{research_type}

<!--
  FIR legend: [F]=Fact  [I]=Inference  [R]=Recommendation
  决策树的每个分支是 [I]（基于 [F] 的条件判断）；叶子节点的推荐是 [R]
  每个叶子必须含"适用边界"，禁止"推荐使用"式的无边界推荐
-->

<!--
  v3.4 增量（C18/C19）：
  - confidence 数字必须紧跟 {cal: <ref-class>} 或 {uncal} 标注；TL;DR 与 Outlook 段禁 {uncal}
  - 跨节单源 [F] 关节（C18 触发）且不可替代时，对应 section 末插 > [SINGLE_SOURCE_RISK]: ... 注解
-->

## 决策树结构

```
根问题：{核心问题 — 例 "该选哪个向量数据库？"}
│
├─ 条件 1：{量化判断条件 — 例 "QPS > 10万？"}
│   ├─ 是 → 分支 A
│   │   └─ 条件 2A：{次级条件 — 例 "是否需要分布式？"}
│   │       ├─ 是 → 方案 A1
│   │       │   └─ [R] 推荐：{方案名称}
│   │       │   └─ [I] 理由：{1-2 句 + 来源 §X}
│   │       │   └─ 适用边界：{具体场景范围}
│   │       │   └─ 不适用：{何时不选 A1}
│   │       └─ 否 → 方案 A2
│   │           └─ [R] 推荐：...
│   │           └─ [I] 理由 / 适用 / 不适用
│   └─ 否 → 分支 B
│       ├─ 条件 2B：{次级条件}
│       │   ├─ 是 → 方案 B1
│       │   └─ 否 → 方案 B2
│       └─ ...
```

## 使用说明

1. **从根节点开始**，按量化条件逐层判断
2. **到达叶子节点**即为推荐方案；叶子标注 `[R] 推荐：<方案>`
3. 若无法判断某条件（数据不足） → 先补数据再走下一层，不要猜

## 边界条件

**本决策树不适用** 的场景：

- {特殊场景 1：例 "若场景是实时音视频，延迟敏感度比 QPS 更关键"}
- {特殊场景 2}

**需人工评估** 的场景：

- {场景 1：例 "若预算 < $10k/月，本树的所有方案都不符合成本约束"}
- {场景 2}

## 证据支持

每个条件 + 叶子的证据来源：

| 条件 / 叶子 | 证据 | 来源 |
|-----|------|------|
| 条件 1 阈值（QPS > 10万） | 方案 A 在 QPS < 5万 时 P99 < 50ms；方案 B 在 QPS > 15万 时优势明显 | [Benchmark 报告](url) |
| 方案 A1 推荐理由 | 分布式支持 + 生产案例 ×3 | [case study](url) |
| ... | ... | ... |

## Skeleton 对齐

本决策树的条件与 `skeleton.dimensions` 对齐：

| 条件 | 对应 dimension | 条件来源 |
|------|---------------|---------|
| 条件 1 | {dim name} | 报告 §X 对比表 |
| 条件 2A | {dim name} | 报告 §Y 分析 |

## 验证信号

本决策树的每个叶子推荐，若实际落地后发现：

- 条件判断阈值需调整 → 更新树并重算
- 新方案出现 → 在对应分支末追加
- 某叶子的"不适用"范围变大 → 降级该推荐或移除

---

> Generated by [forge/insight-fuse](https://github.com/juserai/forge) — Decision tree output
